# Copyright by BeeX [2026]
# foxglove_bridge. Loaded into the foxglove node.

port: 8765
address: 0.0.0.0
send_buffer_limit: 100000000             # bytes queued per client before messages are dropped; one 800x600 cloud is several MB and the 10 MB default overflows
